<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container">
        <!-- Brand Logo and Name -->
        <a class="navbar-brand fw-bold d-flex align-items-center" href="/">
            <img src="/images/icon.png" alt="DriveMate Logo" height="40" class="me-2 rounded">
            <div class="d-flex flex-column">
                <span class="fw-bold fs-5">DriveMate</span>
                <small class="text-warning opacity-75" style="font-size: 0.7rem; line-height: 1;">RoadSide Assistance</small>
            </div>
        </a>

        <!-- Mobile Navigation -->
        <div class="d-lg-none">
            <% if (!user) { %>
                <!-- Show Login button on mobile for all pages except login/register -->
                <a href="/auth/login" class="btn btn-outline-light btn-sm mobile-login-btn">Login</a>
            <% } else { %>
                <!-- Toggle button for logged-in users -->
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
            <% } %>
        </div>

        <!-- Desktop Navigation -->
        <div class="collapse navbar-collapse d-lg-block" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
                <% if (user) { %>
                    <!-- Notification Bell -->
                    <li class="nav-item dropdown me-2">
                        <a class="nav-link position-relative" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" style="color: #FFEB3B !important;">
                            🔔
                            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" 
                                  id="notificationCount" style="display: none; font-size: 0.6em; min-width: 18px; height: 18px; line-height: 1;">
                                0
                            </span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" style="width: 350px; max-height: 400px; overflow-y: auto;" 
                            id="notificationDropdown">
                            <li><h6 class="dropdown-header">Notifications</h6></li>
                            <div id="notificationList">
                                <li class="text-center py-3 text-muted">Loading notifications...</li>
                            </div>
                        </ul>
                    </li>

                    <!-- User dropdown for desktop -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="navbarDropdown" 
                           role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <div class="d-flex flex-column text-end me-2">
                                <span class="fw-bold"><%= user.name %></span>
                            </div>
                            <i class="fas fa-user-circle fs-5"></i>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="/auth/profile">
                                <i class="fas fa-user me-2"></i>My Profile
                            </a></li>
                            <li><a class="dropdown-item" href="/dashboard">
                                <i class="fas fa-tachometer-alt me-2"></i>Dashboard
                            </a></li>
                            
                            <!-- Provider Specific Dropdown Items -->
                            <% if (user.role === 'fuel-station' || user.role === 'mechanic') { %>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/provider/dashboard">
                                    <i class="fas fa-briefcase me-2"></i>Provider Dashboard
                                </a></li>
                                <li><a class="dropdown-item" href="/provider/edit-profile">
                                    <i class="fas fa-edit me-2"></i>Edit Business Profile
                                </a></li>
                            <% } %>
                            
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="/auth/logout">
                                <i class="fas fa-sign-out-alt me-2"></i>Logout
                            </a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <!-- Desktop Login for non-authenticated users -->
                    <li class="nav-item d-none d-lg-block">
                        <a class="nav-link" href="/auth/login">
                            <i class="fas fa-sign-in-alt me-1"></i>Login
                        </a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>

<script>
// Hide mobile login button on login/register pages
document.addEventListener('DOMContentLoaded', function() {
    const currentPath = window.location.pathname;
    const mobileLoginBtn = document.querySelector('.mobile-login-btn');
    
    // Hide login button if we're already on login/register pages
    if (mobileLoginBtn && (currentPath === '/auth/login' || currentPath === '/auth/register')) {
        mobileLoginBtn.style.display = 'none';
    }
});
</script>