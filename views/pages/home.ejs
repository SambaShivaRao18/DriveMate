<%- include('../partials/header') %>
    <%- include('../partials/navbar') %>

        <main class="container mt-5">
            <div class="row">
                <div class="col-lg-8 mx-auto text-center">
                    <!-- Main heading with logo - smaller size -->
                    <div class="d-flex justify-content-center align-items-center mb-4">
                        <img src="/images/icon.png" alt="DriveMate Logo" height="50" class="me-3">
                        <div class="d-flex justify-content-center align-items-center mb-4">
                            <!-- <img src="/images/Logo2.png" alt="DriveMate Logo" height="50" class="me-3"> -->
                            <div class="d-flex align-items-baseline">
                                <h3 class="mb-0 fw-bold me-2">DriveMate</h3>
                                <span class="text-muted">- RoadSide Assistance</span>
                            </div>
                        </div>
                    </div>

                    <p class="lead mb-4">Get immediate help for fuel emergencies and vehicle breakdowns</p>

                    <div class="row mt-5">
                        <div class="col-md-6 mb-4">
                            <div class="card h-100">
                                <div class="card-body">
                                    <h5 class="card-title">⛽ Fuel Assistance</h5>
                                    <p class="card-text">Run out of fuel? Get immediate fuel delivery to your location.
                                    </p>
                                    <% if (user) { %>
                                        <a href="/dashboard?service=fuel" class="btn btn-primary">Request Fuel</a>
                                        <% } else { %>
                                            <a href="/auth/register" class="btn btn-outline-primary">Sign Up to Request</a>
                                            <% } %>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6 mb-4">
                            <div class="card h-100">
                                <div class="card-body">
                                    <h5 class="card-title">🔧 Mechanical Help</h5>
                                    <p class="card-text">Vehicle breakdown? Find nearest mechanics and repair shops.</p>
                                    <% if (user) { %>
                                        <a href="/dashboard?service=mechanic" class="btn btn-primary">Request
                                            Mechanic</a>
                                        <% } else { %>
                                            <a href="/auth/register" class="btn btn-outline-primary">Sign Up to Request</a>
                                            <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>

        <%- include('../partials/footer') %>